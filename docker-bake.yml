groups:
  default:
    targets:
      - backend

targets:
  backend:
    context: ./backend
    dockerfile: ./backend/Dockerfile
    platforms:
      - linux/amd64
      - linux/arm64
    tags:
      - itzthedockerjules/mensarater:backend-0.1.0
      - itzthedockerjules/mensarater:backend-latest
